{% for path in script()  %}
  {% if '//' in path %}
    <script src="{{ path ~ '?' ~ asset_version('')}}"></script>
  {% else %}
    <script src="{{ asset('assets/admin/' ~ path) }}"></script>
  {% endif %}
{% endfor %}