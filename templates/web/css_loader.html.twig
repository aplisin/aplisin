{% for path in css() %}
  {% if path starts with 'http://' %}
    <link href="{{ path }}" rel="stylesheet" />
  {% else %}
    <link href="{{ asset('assets/css/' ~ path) }}" rel="stylesheet" />
  {% endif %}
{% endfor %}