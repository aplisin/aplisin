{% for path in css() %}
  {% if path starts with 'http://' %}
    <link href="{{ path ~ '?' ~ asset_version('') }}" rel="stylesheet"/>
  {% else %}
    <link href="{{ asset('assets/' ~ path) }}" rel="stylesheet"/>
  {% endif %}
{% endfor %}